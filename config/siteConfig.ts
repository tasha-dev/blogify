// Codes by mahdi tasha
// Defining the site config
const siteConfig = {
    description: 'a dynamic blog application leveraging the DEV.to API to fetch a curated selection of blog posts.',
    url: 'https://blogify-plhvfkraz-mahdi-tashas-projects.vercel.app/',
    name: 'Blogify | Mahdi Tasha'
};

// Exporting site config as default
export default siteConfig;