// Codes by mahdi tasha
// Importing type of configs of nextJS
/** @type {import('next').NextConfig} */

// Defining the configs
const nextConfig = {};

// Exporting the configs
export default nextConfig;
